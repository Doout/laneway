# Rust relay interoperability

This opt-in Go suite builds the Rust relay and tests static and
controller-backed Go Node sessions through it. Its build tag keeps the normal
Go suite independent of Rust.

Run it from the repository root as documented in the
[integration test guide](../../../integration/README.md).
